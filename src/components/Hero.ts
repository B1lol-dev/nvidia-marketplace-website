import { Container } from "./Container";

export const Hero = () => {
  return /*html*/ `
    <section>
        ${Container(
          "",
          /*html*/ `
                    
        `
        )}
    </section>
  `;
};
